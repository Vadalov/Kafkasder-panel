# ===================================
# VERCEL ENVIRONMENT VARIABLES TEMPLATE
# ===================================
# Bu dosya Vercel Dashboard'da Settings > Environment Variables
# bölümüne eklenmesi gereken değişkenleri içerir.
#
# KULLANIM:
# 1. Bu dosyayı açın
# 2. Değerleri kendi bilgilerinizle doldurun
# 3. Vercel Dashboard'da manuel olarak ekleyin
# VEYA
# 4. Vercel CLI ile otomatik ekleyin (aşağıdaki komutlar)
#
# CLI ile ekleme örneği:
# vercel env add NEXT_PUBLIC_CONVEX_URL production
# (Terminal size değeri soracak, yapıştırın ve Enter'a basın)
# ===================================

# ===================================
# ZORUNLU VARIABLES (PRODUCTION)
# ===================================

# Convex Backend URL
# Convex Dashboard'dan alın: https://dashboard.convex.dev
# Örnek: https://happy-animal-123.convex.cloud
NEXT_PUBLIC_CONVEX_URL=

# CSRF Protection Secret (32+ karakter)
# Oluşturma: openssl rand -base64 32
CSRF_SECRET=

# Session Secret (32+ karakter)
# Oluşturma: openssl rand -base64 32
SESSION_SECRET=

# Node Environment
NODE_ENV=production

# İlk Admin Kullanıcı (İlk deployment için)
# Seed script bu bilgileri kullanacak
FIRST_ADMIN_EMAIL=baskan@dernek.org
FIRST_ADMIN_PASSWORD=

# ===================================
# ÖNERİLEN VARIABLES (MONITORING)
# ===================================

# Sentry Error Tracking
# https://sentry.io - Proje oluşturun ve DSN'i alın
NEXT_PUBLIC_SENTRY_DSN=
SENTRY_DSN=
SENTRY_ORG=
SENTRY_PROJECT=kafkasder-panel
SENTRY_AUTH_TOKEN=

# ===================================
# OPSİYONEL SERVISLER
# ===================================

# --- EMAIL SERVICE (SMTP) ---
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=
SMTP_PASSWORD=
SMTP_FROM=noreply@kafkasder.org

# --- SMS SERVICE (Twilio) ---
TWILIO_ACCOUNT_SID=
TWILIO_AUTH_TOKEN=
TWILIO_PHONE_NUMBER=

# --- WHATSAPP ---
# İlk deployment'ta false olmalı
# QR kod tarayıp bağlantı kurulduktan sonra true yapın
WHATSAPP_AUTO_INIT=false

# --- AI CHAT (OpenAI) ---
OPENAI_API_KEY=

# --- GOOGLE MAPS ---
NEXT_PUBLIC_GOOGLE_MAPS_API_KEY=

# --- N8N WEBHOOKS (Automation) ---
N8N_DONATION_WEBHOOK_URL=
N8N_ERROR_WEBHOOK_URL=
N8N_TELEGRAM_WEBHOOK_URL=
N8N_WEBHOOK_SECRET=

# --- ANALYTICS ---
NEXT_PUBLIC_GA_MEASUREMENT_ID=

# --- FEATURE FLAGS ---
NEXT_PUBLIC_ENABLE_REALTIME=true
NEXT_PUBLIC_ENABLE_ANALYTICS=true
NEXT_PUBLIC_DEMO_MODE=false

# --- RATE LIMITING ---
RATE_LIMIT_MAX_REQUESTS=100
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_PREMIUM_MULTIPLIER=2.0
RATE_LIMIT_WHITELIST_IPS=
RATE_LIMIT_BLACKLIST_IPS=

# --- SESSION MANAGEMENT ---
NEXT_PUBLIC_MAX_CONCURRENT_SESSIONS=3
NEXT_PUBLIC_SESSION_INACTIVITY_TIMEOUT=3600000
NEXT_PUBLIC_SESSION_MAX_AGE=86400000

# --- FILE UPLOADS ---
MAX_FILE_SIZE=10485760
MAX_FILES_PER_UPLOAD=5

# ===================================
# VERCEL CLI İLE TOPLU EKLEME
# ===================================
#
# Önce bu dosyayı .env.production olarak kopyalayın ve değerleri doldurun:
# cp .env.vercel.template .env.production
#
# Sonra Vercel CLI ile otomatik yükleyin:
# vercel env pull .env.local    # Mevcut değerleri çek
# vercel env add                # Tek tek eklemek için
#
# VEYA aşağıdaki script'i kullanın (bash):
#
# #!/bin/bash
# while IFS='=' read -r key value; do
#   if [[ ! $key =~ ^# ]] && [[ -n $key ]] && [[ -n $value ]]; then
#     echo "Adding $key to production..."
#     echo "$value" | vercel env add "$key" production
#   fi
# done < .env.production
#
# ===================================

# ===================================
# ENVIRONMENT SCOPE BİLGİSİ
# ===================================
# Vercel'de her variable için 3 scope var:
#
# 1. Production: Canlı site (main branch)
#    - Tüm zorunlu variables production'da OLMALI
#
# 2. Preview: Pull request ve feature branch deployments
#    - Test için gerekli variables (genelde production ile aynı)
#    - Test API keys kullanabilirsiniz
#
# 3. Development: Lokal geliştirme
#    - .env.local kullanmanız önerilir
#    - Vercel'e eklemenize gerek yok
#
# ÖNERİ: Production ve Preview için aynı değerleri kullanın
# (test API keys kullanıyorsanız farklı olabilir)
# ===================================

# ===================================
# GÜVENLİK NOTLARI
# ===================================
#
# ⚠️ BU DOSYAYI GIT'E COMMIT ETMEYİN!
# ⚠️ Doldurulan değerleri kimseyle paylaşmayın
# ⚠️ Secrets'i düzenli olarak rotate edin
# ⚠️ Production ve development için farklı secrets kullanın
# ⚠️ API keys için minimum gerekli permissions verin
# ⚠️ Rate limiting'i aktif tutun
#
# ✅ .gitignore'da .env.production var mı kontrol edin
# ✅ Team'den ayrılan kişiler için secrets'i rotate edin
# ✅ Vercel Dashboard'da access control yapın
# ✅ 2FA aktif edin (Vercel ve Convex hesapları için)
#
# ===================================
